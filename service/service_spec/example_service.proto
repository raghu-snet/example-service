syntax = "proto3";

package example_service;

message Message {
    string greetingMsg = 1;
    string name = 2;
}

message Result {
    string message = 1;
}

service Greeter {
    rpc welcome(Message) returns (Result) {}
    rpc hello(Message) returns (Result) {}
}